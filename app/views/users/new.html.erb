<% provide(:title, 'Sign Up') %>
<div class="container-fluid regular-background">
    <!-- new user -->
    <%= render 'layouts/flash_messages' %>
    <div class="row">
        <div class="panel panel-default remove-padding">
            <div class="panel-heading text-center"><h1>Sign Up</h1></div>
            <div class="panel-body">
                <%= render 'users/fields' %>
            </div>
        </div>
    </div>
</div>
