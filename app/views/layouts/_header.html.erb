
<nav class="navbar navbar-default  navbar-static-top navbar-carmante">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Carmanate</a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                <li <%= 'class="active"' if @active == 'home'  %>><a href="/">Home</a></li>
                <li <%= 'class="active"' if @active == 'about' %>><a href="/about">About</a></li>

                <% if signed_in? %>
                    <li><%= link_to "Sign Out", signout_path, method: 'delete' %></li>
                <% else %>
                    <li><%= link_to "Sign Up", signup_path %></li>
                    <li><%= link_to "Sign In", signin_path %></li>
                <% end %>

            </ul>
        </div>
    </div>
</nav>
<% if signed_in? %>
<nav class="navbar navbar-default  navbar-static-top navbar-carmante navbar-lower">
    <div class="container-fluid">
        <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="#"><%= current_user.name %></a>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
                <li><%= link_to "Create Car Profile", new_car_profile_path(current_user) %></li>
                <li><%= link_to "Car Profiles", user_path(current_user) %></li>
                <li><%= link_to "Account Info", edit_user_path(current_user) %></li>
            </ul>
        </div>
    </div>
</nav>
<% end %>